<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <artifactId>layer-generator</artifactId>
        <groupId>com.github.ma_vin.util.layer_generator</groupId>
        <version>2.1.0-SNAPSHOT</version>
    </parent>

    <packaging>pom</packaging>
    <artifactId>sample</artifactId>

    <name>Util Layer Generator Sample</name>
    <description>Examples of usage of plugin, annotations and configurations</description>

    <modules>
        <module>util-sample</module>
        <module>plugin-sample</module>
        <module>annotation-sample</module>
        <module>entity-sample</module>
        <module>grouping-sample</module>
        <module>field-sample</module>
        <module>reference-sample</module>
        <module>extension-sample</module>
        <module>version-sample</module>
    </modules>

    <properties>
        <model.file.baseName>exampleModel</model.file.baseName>
        <model.file.ending>yaml</model.file.ending>
        <model.generate.dto>true</model.generate.dto>
        <model.generate.domain>true</model.generate.domain>
        <model.generate.dao>true</model.generate.dao>
        <!-- these classes are generated and analysed by module plugin-sample -->
        <sonar.cpd.exclusions>**/AbstractMapper.java,**/AbstractAccessMapper.java,**/AbstractTransportMapper.java</sonar.cpd.exclusions>
    </properties>


    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>com.github.ma_vin.util.layer_generator</groupId>
                    <artifactId>generator-plugin</artifactId>
                    <executions>
                        <execution>
                            <goals>
                                <goal>generate-model</goal>
                            </goals>
                        </execution>
                    </executions>
                    <configuration>
                        <!-- src is used only for demonstration. Some target or like src-gen folder should be preferred -->
                        <generateTargetDirectory>src/main/generated/plugin/java/</generateTargetDirectory>
                        <generateDto>${model.generate.dto}</generateDto>
                        <generateDomain>${model.generate.domain}</generateDomain>
                        <generateDao>${model.generate.dao}</generateDao>
                        <modelDefinitionDirectory>src/main/resources/</modelDefinitionDirectory>
                        <modelDefinitionFilename>${model.file.baseName}.${model.file.ending}</modelDefinitionFilename>
                        <cleanTargetDirectory>false</cleanTargetDirectory>
                        <cleanBasePackage>true</cleanBasePackage>
                    </configuration>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-dependency-plugin</artifactId>
                    <executions>
                        <execution>
                            <id>unpack</id>
                            <phase>generate-sources</phase>
                            <goals>
                                <goal>unpack</goal>
                            </goals>
                            <configuration>
                                <artifactItems>
                                    <artifactItem>
                                        <groupId>com.github.ma_vin.util.layer_generator</groupId>
                                        <artifactId>generator-plugin</artifactId>
                                        <version>${project.version}</version>
                                        <outputDirectory>${project.build.directory}</outputDirectory>
                                        <includes>**\/*.xsd</includes>
                                    </artifactItem>
                                </artifactItems>
                            </configuration>
                        </execution>
                    </executions>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-resources-plugin</artifactId>
                    <executions>
                        <execution>
                            <id>copy-resources</id>
                            <phase>generate-sources</phase>
                            <goals>
                                <goal>copy-resources</goal>
                            </goals>
                            <configuration>
                                <outputDirectory>${project.basedir}/src/main/resources</outputDirectory>
                                <resources>
                                    <resource>
                                        <directory>${project.build.directory}/xsd</directory>
                                        <filtering>true</filtering>
                                        <includes>
                                            <include>*.xsd</include>
                                        </includes>
                                    </resource>
                                </resources>
                            </configuration>
                        </execution>
                    </executions>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <executions>
                        <execution>
                            <id>default-compile</id>
                            <configuration>
                                <generatedSourcesDirectory>${project.basedir}/src/main/generated/annotation/java
                                </generatedSourcesDirectory>
                                <compileSourceRoots>
                                    ${project.build.sourceDirectory},${project.basedir}/src/main/generated/plugin/java
                                </compileSourceRoots>
                            </configuration>
                        </execution>
                    </executions>
                </plugin>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-clean-plugin</artifactId>
                    <executions>
                        <execution>
                            <id>clean-generated</id>
                            <phase>clean</phase>
                            <goals>
                                <goal>clean</goal>
                            </goals>
                            <configuration>
                                <filesets>
                                    <fileset>
                                        <directory>${project.basedir}/src/main/generated/annotation/java</directory>
                                    </fileset>
                                </filesets>
                            </configuration>
                        </execution>
                    </executions>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>